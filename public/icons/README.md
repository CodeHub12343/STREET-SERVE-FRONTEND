# PWA Icons

Placeholder location for the PWA icons referenced by `src/app/manifest.ts`:

- `icon-192.png` (192×192)
- `icon-512.png` (512×512)
- `maskable-512.png` (512×512, `purpose: maskable`)

These are produced from the finalized StreetServe brand mark in **Milestone 9 (PWA hardening)**
— see `PWA_IMPLEMENTATION.md §2`. Until then the manifest references resolve to 404s, which is
harmless (the app is installable once the assets land).
