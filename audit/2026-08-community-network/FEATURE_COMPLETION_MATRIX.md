# Feature Completion Matrix — Community Network Specification

Every requirement extracted from the specification, with the twelve tracking fields the audit brief requires.

**Legend**
- **Status:** Complete · Partial · Missing · Needs Fixing · Needs Refactoring · Recommended Improvement
- **FE / BE:** ✅ done · ◐ partial · ✗ absent · — not applicable
- **Pri:** P0 blocker · P1 high · P2 medium · P3 low
- **Cx:** S (≤2 d) · M (3–8 d) · L (2–4 wk) · XL (>1 mo)

Percentages are weighted 50/50 frontend/backend unless the requirement is backend-only. A non-zero
percentage on a Missing row credits **reusable existing infrastructure**, never partial delivery.

---

## A. Delivery Assist Network

| ID | Feature / description | Status | FE | BE | % | Pri | Cx | Issues found | Required work · dependencies · next step |
|---|---|---|---|---|---|---|---|---|---|
| DAN-1 | **"Need Delivery Help" request** — vendor taps a button on an active order to request a driver | Missing | ✗ | ✗ | 0 | P1 | M | No delivery request entity anywhere. Vendor order surface is [`vendor/orders`](../../src/app/(dashboard)/vendor/orders/) with accept/ready/complete only | Create `delivery_requests` + `POST /deliveries`. Deps: DAN-3, DAN-10, [ADR-004](ADR-004-driver-classification-and-liability.md) ✅. **Next:** model it on `WaveDownSchema`, which is the same request/accept/expire shape with a snapshotted fee |
| DAN-2 | **Radius broadcast to nearby drivers** — alert available drivers within a chosen radius | Missing | ✗ | ✗ | 10 | P1 | M | No driver availability concept. But the fan-out primitive is built and proven: `$near` over live positions + Redis `setNx` throttle at [`livemap.service.ts:578-607`](../../../STREET-SERVE-APPLICATION-BACKEND/src/modules/livemap/livemap.service.ts#L578-L607) | Add driver on-shift sessions (reuse `live_sessions` with a new `actor_type`), then fan out. Deps: DAN-3. **Next:** [A-5](ARCHITECTURAL_IMPROVEMENTS.md) settles it — a `live_sessions` actor, excluded from customer map queries. Broadcast must be **event-driven**, and ADR-004 prohibits assignment |
| DAN-3 | **Driver role, onboarding and vetting** — identity, licence, insurance, background check | Missing | ✗ | ✗ | 15 | P0 | L | `ROLES` ([`constants.ts:7-16`](../../../STREET-SERVE-APPLICATION-BACKEND/src/config/constants.ts#L7-L16)) has no driver. Verification tiers (`tier0/bronze/silver/gold`) and the `identity` module give the vetting spine; no vehicle, licence, or insurance entity exists | Add `driver` role + a `driver_profiles` doc (vehicle, licence expiry, insurance expiry, check status). Deps: [ADR-004](ADR-004-driver-classification-and-liability.md) ✅ (insurance + counsel still gating). **Next:** `silver` tier + licence + background check + insurance attestation; role **never** self-grantable, and fix the inverted comment ([F-7](FEATURES_REQUIRING_FIXES.md)) before adding it |
| DAN-4 | **First-to-accept claim** — the first driver to accept gets the delivery | Missing | ✗ | ✗ | 5 | P1 | S | Nothing to race on yet. The correct pattern already exists: `findOneAndUpdate` with a status guard, as used for wave acceptance and queue holds | Atomic claim on `{_id, status:'broadcasting'}`. Deps: DAN-1. **Next:** write the losing-racer test first — it is the only interesting case |
| DAN-5 | **Pickup + destination display for the driver** | Missing | ✗ | ✗ | 5 | P1 | S | No driver app surface at all | Driver offer card with both pins, distance, and payout. Deps: DAN-1, DAN-10. **Next:** reuse the Mapbox components in `features/livemap/` |
| DAN-6 | **Live GPS tracking for the customer** | Missing | ✗ | ✗ | 20 | P1 | L | The realtime emit surface ([`hub.ts:28-53`](../../../STREET-SERVE-APPLICATION-BACKEND/src/realtime/hub.ts#L28-L53)) has no courier channel. Wave-Down, the nearest analogue, gives the customer an **ETA integer only** — [`useWave.ts:71`](../../src/features/wave/hooks/useWave.ts#L71) — not a moving pin. `location_pings` (30-day TTL) + `liveStore` Redis mirror are directly reusable | Add a `/delivery` namespace, `delivery:position`, and a customer tracking view. Deps: DAN-1, DAN-4. **Next:** load-model it first — see [PRODUCTION_READINESS_REPORT.md](PRODUCTION_READINESS_REPORT.md) §2 |
| DAN-7 | **Delivery lifecycle state machine** — requested → broadcasting → accepted → picked_up → delivered, plus cancel/expire | Missing | ✗ | ✗ | 0 | P1 | M | None | Explicit state machine with guarded transitions + an SLA sweep. Deps: DAN-1. **Next:** copy the `wave-down-sla` scheduler entry pattern for expiry |
| DAN-8 | **Delivery coordination fee** — platform revenue per completed delivery | Missing | — | ✗ | 25 | P1 | S | `FEE_TYPES` ([`constants.ts:200-213`](../../../STREET-SERVE-APPLICATION-BACKEND/src/config/constants.ts#L200-L213)) has twelve types, none for delivery. The registry is designed for exactly this — adding a type is config plus one call site | Add `delivery_coordination` to `FEE_TYPES` + `DEFAULT_FEE_RULES`, seed a rate, apply at completion. Deps: DAN-7. **Next:** price it flat, not percentage — `wave_convenience` ([`constants.ts:250`](../../../STREET-SERVE-APPLICATION-BACKEND/src/config/constants.ts#L250)) already argues why dispatch shouldn't scale with basket size |
| DAN-9 | **Driver payout** | Missing | ✗ | ✗ | 40 | P1 | M | The rail exists and works: `POST /jobs/:id/check-out` triggers a Stripe Connect transfer with `payout_ref`/`payout_cents`, plus `payout-retry` and reconciliation sweeps | Trigger the same rail on delivery completion. Deps: DAN-7, DAN-3 (Connect onboarding). **Next:** confirm whether a driver payout should be immediate or batched daily; immediate matches the "Earn Today" promise |
| DAN-10 | **Delivery as an order fulfilment mode** — address capture, delivery totals | Missing | ✗ | ◐ | 15 | P0 | M | `fulfillment_type` is `['pickup_now','pickup_scheduled']` only ([`orders.model.ts:23-26`](../../../STREET-SERVE-APPLICATION-BACKEND/src/modules/orders/orders.model.ts#L23-L26)). `delivery_cents` exists at line 47 but the adjacent comment states it is $0 in the pickup MVP. **No order can carry a destination address** | Add `delivery` to the enum + a destination subdocument + address validation. Deps: none. **Next:** this is the true prerequisite for the whole feature — do it first and independently |
| DAN-11 | **Customer-facing delivery pricing and disclosure** | Missing | ✗ | ✗ | 20 | P1 | M | Checkout itemisation is complete and good (previous audit RV-15); it simply has no delivery line. Fee snapshotting discipline is established by `WaveDownSchema` | Quote at checkout, snapshot on the order, itemise. Deps: DAN-8, DAN-10. **Next:** snapshot the quote — a mid-flight re-price is the exact bug the wave travel fee comment warns about |
| DAN-12 | **Proof of delivery** — photo or code at hand-off | Missing | ✗ | ✗ | 20 | P3 | S | None. The rotating-HMAC QR in [`jobQr.ts`](../../../STREET-SERVE-APPLICATION-BACKEND/src/modules/jobs/jobQr.ts) and the S3 presign flow in `storage` are both directly reusable | Photo upload or a customer-shown code. Deps: DAN-7. **Next:** start with a code; photos add a moderation surface |
| DAN-13 | **Failure paths** — nobody accepts, driver cancels, customer unreachable | Missing | ✗ | ✗ | 10 | P1 | M | None. `WaveDownSchema` models `declined/expired/cancelled` with `decline_reason`, which is the right precedent | Expiry sweep + re-broadcast + refund path. Deps: DAN-7, refunds module (built). **Next:** nobody is charged before acceptance (ADR-004 §D). A decline is free and untracked — acceptance-rate pressure is prohibited |
| DAN-14 | **Multi-vertical support** — mechanics, groomers, florists, not just food | Missing | ✗ | ✗ | 30 | P2 | M | The `ARCHETYPES` system ([`constants.ts:47-52`](../../../STREET-SERVE-APPLICATION-BACKEND/src/config/constants.ts#L47-L52)) already makes "support every business type" tractable; delivery would attach to `counter_serve` and `goods_seller` naturally, and is meaningless for `appointment_service` | Expose delivery as an archetype-gated module. Deps: DAN-1, module system (built). **Next:** add it to the module registry rather than hardcoding category checks |
| DAN-15 | **Driver earnings and history surface** | Missing | ✗ | ✗ | 35 | P2 | M | `/seller/earnings`, `/seller/balance`, and the earn hub exist and are the right shape | Driver-scoped earnings view. Deps: DAN-9. **Next:** reuse the earn hub rather than building a parallel dashboard |
| DAN-16 | **Driver classification, liability and insurance** | Missing | — | — | 0 | P0 | L | [ADR-002](../2026-08-marketplace-spec/ADR-002-staff-vs-gig.md) fixed engagements-not-employment with an enforced copy rule; dispatch pressure (acceptance rates, assignment) pushes against it. Third-party physical risk is new to this platform. **No insurance discussion exists in either repo** | **[ADR-004](ADR-004-driver-classification-and-liability.md) ✅ decided:** engagements; assignment / acceptance-rate pressure / exclusivity prohibited; platform provides no driver cover; vetting + non-self-grantable role. **Remaining: insurance must be quoted and bound, and counsel must review the driver terms.** **Next:** those two are people-gates, not commits — start them now, they are the long pole |

## B. Pay It Forward

| ID | Feature / description | Status | FE | BE | % | Pri | Cx | Issues found | Required work · dependencies · next step |
|---|---|---|---|---|---|---|---|---|---|
| PIF-1 | **Per-business enable toggle** | Missing | ✗ | ✗ | 30 | P1 | S | No setting exists. The business module system + `vendor/modules` + `vendor/settings` are exactly the right home | Add a `pay_it_forward` module with settings. Deps: none. **Next:** register it as a module so archetype defaults apply |
| PIF-2 | **Money pool balance per business** | Missing | ✗ | ✗ | 15 | P0 | M | No pool. **Accounting gap:** `ACCOUNT_TYPES` ([`ledger.model.ts:19-32`](../../../STREET-SERVE-APPLICATION-BACKEND/src/modules/ledger/ledger.model.ts#L19-L32)) has no custodial community-fund type; `payable` means "owed to a seller" and misusing it corrupts payout maths | `community_fund_payable` account type + a per-business fund doc. Deps: **ADR-005 (blocking)**. **Next:** ledger change first, feature second |
| PIF-3 | **Contribute preset or custom amounts** | Missing | ✗ | ✗ | 30 | P1 | S | No contribution path. Payment intent + idempotency + webhook capture are all built and proven | Contribution intent → webhook → credit pool. Deps: PIF-2. **Next:** mirror `PingBudgetTopup` ([`growth.model.ts:26-44`](../../../STREET-SERVE-APPLICATION-BACKEND/src/modules/growth/growth.model.ts#L26-L44)) — it exists *because* crediting a balance without collecting money spent platform capital. Same trap, same fix |
| PIF-4 | **Redemption at checkout** — offer the balance, deduct, show remainder | Missing | ✗ | ✗ | 10 | P0 | M | None. Discount application in [`orders/discounts.ts`](../../../STREET-SERVE-APPLICATION-BACKEND/src/modules/orders/discounts.ts) is the right insertion point | Redemption step in the order pricing pipeline. Deps: PIF-2, PIF-9, PIF-10. **Next:** decide ordering vs discounts — the fund should apply **after** discounts, or generosity subsidises the vendor's own promo |
| PIF-5 | **Product pool** — buy an item that waits in inventory for someone | Missing | ✗ | ◐ | 25 | P2 | M | `GiveawaySchema` ([`growth.model.ts:99-113`](../../../STREET-SERVE-APPLICATION-BACKEND/src/modules/growth/growth.model.ts#L99-L113)) already models product name + daily cap + claimed counter + one-claim-per-user-per-day. It is **vendor-funded**; this is customer-funded | Extend giveaways with a funding source and a per-unit funder. Deps: PIF-2. **Next:** defer to Phase 2 — money pools deliver the same value with far less inventory coupling |
| PIF-6 | **Partial payment** — "$5 toward the next person's order" | Missing | ✗ | ✗ | 5 | P2 | S | None | A capped partial application of the pool. Deps: PIF-4. **Next:** falls out of PIF-4 if redemption is written as `min(balance, cap, total)` from the start |
| PIF-7 | **Anonymous gift** | Missing | ✗ | ✗ | 10 | P2 | S | None. Contact hashing precedent exists in `pings`/`gifts` | Anonymity flag honoured in every read path. Deps: PIF-3. **Next:** enforce at serialisation, not in the UI |
| PIF-8 | **Public recognition** — "provided by James" / business sponsor credit | Missing | ✗ | ✗ | 5 | P2 | S | None | Opt-in display name on the contribution. Deps: PIF-7. **Next:** default to anonymous; recognition is opt-**in**, and the reverse is a privacy incident |
| PIF-9 | **Business caps and settings** — max per redemption, per customer, per day, percentage; money/product/both; expiry | Missing | ✗ | ✗ | 20 | P0 | M | None. Cap patterns are everywhere in `growth` (daily caps, per-user caps) and reusable | Settings doc + enforcement in the redemption path. Deps: PIF-1. **Next:** caps must be enforced server-side in the same transaction as the deduction |
| PIF-10 | **Anti-fraud** — one redemption/account/business/day, phone, email, GPS, fingerprint, manual approval, AI detection, permanent receipts | Missing | ✗ | ✗ | 45 | P0 | L | Absent for PIF, but the toolkit is the best-developed part of this codebase: unique partial indexes ([`growth.model.ts:64-74`](../../../STREET-SERVE-APPLICATION-BACKEND/src/modules/growth/growth.model.ts#L64-L74)), `device_fingerprint`, daily caps, qualifying actions, a `fraud-signals` sweep, an admin fraud console, and verification tiers | Apply the ping playbook to redemption. Deps: PIF-4. **Next:** the unique index `{business_id, user_id, day_key}` is the single highest-value control — copy `GiveawayClaimSchema` verbatim |
| PIF-11 | **Business impact dashboard** — meals given, money shared, people helped | Missing | ✗ | ✗ | 20 | P2 | M | None. `vendor/analytics` is the right home and the aggregation patterns exist | Aggregates + a dashboard panel. Deps: PIF-4. **Next:** compute from immutable receipts, never from a mutable counter |
| PIF-12 | **Platform-wide live counter** | Missing | ✗ | ✗ | 10 | P3 | S | None | A cached global aggregate. Deps: PIF-11. **Next:** cache in Redis with a TTL; this is a marketing number, not a financial one |
| PIF-13 | **Discovery filters** — businesses with PIF / free coffee / community gifts | Missing | ✗ | ✗ | 25 | P2 | M | None. Map + list filtering by category tab is built and extensible | A boolean facet on the map/list query. Deps: PIF-2. **Next:** watch the query cost — the map route is already near its perf budget |
| PIF-14 | **Map icons for available gifts** | Missing | ✗ | ✗ | 30 | P2 | S | None. Pin rendering and the [MAP_REDESIGN_SPECIFICATION](../../MAP_REDESIGN_SPECIFICATION.md) palette work exist | Pin badge variants. Deps: PIF-13. **Next:** three colours on a map is a legibility problem — check contrast against the redesigned basemap |
| PIF-15 | **Notifications** — "someone left you a free coffee" | Missing | ✗ | ✗ | 40 | P2 | S | None for PIF. The notifications module, preference categories, inbox, and push tokens are all built | New notification category + trigger. Deps: PIF-4. **Next:** add the category to `NOTIFICATION_PREF_CATEGORIES` so it is mutable — an unmutable generosity ping becomes spam |
| PIF-16 | **Reporting** — donated/redeemed, averages, largest, repeat donors, tax-friendly | Missing | ✗ | ✗ | 15 | P2 | M | None. The `tax` module exists but covers marketplace facilitator sales tax, not donation receipts | Reports + export. Deps: PIF-11. **Next:** **do not call it tax-deductible.** A vendor is not a 501(c)(3); the correct output is a contribution record, and the wording needs the same copy-rule treatment as `stock_waiver` |
| PIF-17 | **Giver badges on customer profiles** | Missing | ✗ | ✗ | 25 | P3 | M | None. The academy/certification badge system exists and is reusable | Badge definitions + award triggers. Deps: PIF-3. **Next:** anti-gaming — badges awarded on *contributed* money invite wash-giving; award on redeemed-by-others |
| PIF-18 | **Social sharing** | Missing | ✗ | ✗ | 10 | P3 | S | None | Share sheet + OG image. Deps: PIF-3. **Next:** respect PIF-7 anonymity in the share payload |
| PIF-19 | **Corporate sponsorships** — sponsor 500 coffees with branding | Missing | ✗ | ✗ | 20 | P3 | L | `sponsors` module is **launch/landing-page** sponsors (logo, UTM, signup attribution) — a different entity that shares a word | A sponsor-funded pool spanning businesses. Deps: PIF-2, PIF-8. **Next:** defer; multi-business pooling multiplies the custody problem |
| PIF-20 | **Emergency mode / priority groups** — veterans, first responders, teachers, unhoused | Missing | ✗ | ✗ | 20 | P3 | L | None. The `shelter` module has real precedent for serving vulnerable users, and its custody payout rail shows the care level required | Eligibility groups + gated redemption. Deps: PIF-9. **[ADR-005](ADR-005-custodial-community-funds.md) ✅ decided: self-attestation only** — the platform does not adjudicate membership of protected or sensitive classes. **Next:** build as a vendor-set cap on a self-declared group; no document verification, ever |
| PIF-21 | **Public Community Impact page** | Missing | ✗ | ✗ | 10 | P3 | M | None | Public stats page + live feed. Deps: PIF-12. **Next:** a live feed of who gave what is a privacy surface — anonymise by default |
| PIF-22 | **Discounted pay-it-forward rates** — giver sets recipient to pay 10–20% instead of full price | Missing | ✗ | ✗ | 15 | P2 | M | Raised in the brief's addendum. Not modelled. Percentage-based application interacts with the existing discount engine and with PIF-4 ordering | Percentage mode alongside fixed-amount. Deps: PIF-4, PIF-9. **Next:** resolve stacking against queue discounts before building — two multiplicative discounts can reach zero |
| PIF-23 | **Custodial accounting for pooled funds** | Missing | — | ✗ | 30 | P0 | M | The right precedent exists and is well-reasoned: `tax_payable` is documented as *"NEVER revenue and never distributable"* ([`ledger.model.ts:24-31`](../../../STREET-SERVE-APPLICATION-BACKEND/src/modules/ledger/ledger.model.ts#L24-L31)). No equivalent for community funds | New account + entry types, reconciliation coverage. Deps: [ADR-005](ADR-005-custodial-community-funds.md) ✅. **Next:** the true first ticket of this feature — `community_fund_payable`, credit-normal, never withdrawable |
| PIF-24 | **Unredeemed-funds disposition** — expiry (30/60/never) and what happens to the money | Missing | ✗ | ✗ | 5 | P0 | M | The spec offers "never" as an expiry option, which means a **perpetual liability**; several US states treat long-dormant prepaid balances as unclaimed property | **[ADR-005 §6](ADR-005-custodial-community-funds.md) ✅ decided: 12 months, "never" removed.** Expired funds redistribute to city pools — never to the vendor (who would then profit from suppressing redemption) and never to the platform. Deps: PIF-23. **Next:** build the sweep; the escheatment position still needs counsel sign-off |

## C. Boost My Marketing (community-funded direct mail)

| ID | Feature / description | Status | FE | BE | % | Pri | Cx | Issues found | Required work · dependencies · next step |
|---|---|---|---|---|---|---|---|---|---|
| MB-1 | **"Boost My Marketing" campaign creation** | Missing | ✗ | ✗ | 20 | P2 | M | No crowdfunded campaign entity. `PlacementSchema` ([`ads.model.ts`](../../../STREET-SERVE-APPLICATION-BACKEND/src/modules/ads/ads.model.ts)) is a complete self-funded prepaid campaign — reusable in shape, wrong in funding model | `boost_campaigns` collection + create flow. Deps: ADR-006. **Next:** a sibling of `placements`, not a variant — one funder vs many is a different aggregate |
| MB-2 | **Goal, raised, remaining** | Missing | ✗ | ✗ | 15 | P2 | S | None. `budget_cents`/`spent_cents` is the analogous pair | Goal + contributed totals. Deps: MB-1. **Next:** derive `raised` from contribution rows, never from an incremented counter |
| MB-3 | **Contributions from customers, vendors, sponsors** | Missing | ✗ | ✗ | 25 | P2 | M | None. Payment intents + webhooks + idempotency all exist | Contribution flow. Deps: MB-1, [ADR-006](ADR-006-crowdfunding-capture-model.md) ✅. **Next:** capture on contribution into the campaign-scoped custodial balance, `PingBudgetTopup` discipline. No platform fee on this leg |
| MB-4 | **Live postcard estimate** — "$50 ≈ 250 postcards" | Missing | ✗ | ✗ | 0 | P3 | S | No mail-cost model. **Could not confirm** the spec's rate reflects a real quote | Rate config + estimator. Deps: MB-8. **Next:** label it an estimate in the UI and source the rate from config, not code |
| MB-5 | **Goal-reached notifications** (owner + platform team) | Missing | ✗ | ✗ | 40 | P2 | S | None. Notifications + admin notices are built | Trigger on goal. Deps: MB-2. **Next:** trivial once MB-2 lands |
| MB-6 | **Owner confirms mailing date** | Missing | ✗ | ✗ | 10 | P3 | S | None. The `scheduling` module handles availability, not this | Confirmation step. Deps: MB-5 |
| MB-7 | **Mailing-list generation for the service area** | Missing | ✗ | ✗ | 5 | P2 | L | None. Geo targeting exists on placements (`city_slug`, `radius_m`, 2dsphere); **a postal address list is a different product** requiring a licensed data source | Integrate a saturation-mail or list vendor. Deps: MB-8. **Next:** every-door saturation mail avoids buying a consumer address list, and with it a privacy surface. Strongly recommended over targeted lists |
| MB-8 | **Print and mail vendor integration** | Missing | — | ✗ | 0 | P2 | L | No print/mail vendor in [THIRD_PARTY_INTEGRATIONS.md](../../../STREET-SERVE-APPLICATION-BACKEND/THIRD_PARTY_INTEGRATIONS.md). **Could not confirm** any vendor has been evaluated | Select + integrate (Lob or equivalent). Deps: none. **Next:** get one real quote — it sets MB-4's numbers and the whole feature's unit economics |
| MB-9 | **Campaign status tracking** — Preparing → Printing → Mailed → Delivered | Missing | ✗ | ✗ | 15 | P3 | M | None. Placement `status` transitions are the pattern; delivery confirmation depends on vendor webhooks | Status machine fed by vendor webhooks. Deps: MB-8. **Next:** confirm the vendor actually reports delivery — many report *mailed* only, and promising "Delivered" without data is a broken promise in the UI |
| MB-10 | **Unmet-goal disposition** — refund, owner top-up, or roll forward | Missing | ✗ | ✗ | 0 | P1 | M | **The brief raised this and left it open.** Now **decided** by [ADR-006](ADR-006-crowdfunding-capture-model.md), so it no longer blocks | ≤60-day deadline sweep; automatic full refund (platform absorbs processing cost); owner top-up before the deadline only; roll-forward opt-in at contribution time. Deps: MB-1. **Next:** build the deadline sweep alongside the campaign entity, not after |
| MB-11 | **Contributor recognition** | Missing | ✗ | ✗ | 10 | P3 | S | None | Opt-in contributor list. Deps: MB-3. **Next:** share the anonymity model with PIF-7 |
| MB-12 | **Escrow / custodial accounting for contributions** | Missing | — | ✗ | 20 | P1 | S | Same gap as PIF-23, and **resolved the same way**. The undefined holding period is closed by MB-10's deadline | Reuse `community_fund_payable`, campaign-scoped — **no new account type**. Deps: X-2 (built in Phase 2 for PIF anyway). **Next:** this shrank from M to S once ADR-006 chose to share PIF's rail |
| MB-13 | **Regulatory posture** — escrow, money transmission, refund obligations | Missing | — | — | 0 | P0 | M | Not addressed anywhere. Holding third-party funds against an undelivered service is escrow-shaped | Counsel review. Deps: none. **Next:** bundle with the ADR-005 conversation — same lawyer, same question. [ADR-006](ADR-006-crowdfunding-capture-model.md) narrows it: the no-withdrawal rule is designed to keep the money-transmitter answer "no" |

## D. Cross-cutting requirements

| ID | Feature / description | Status | FE | BE | % | Pri | Cx | Issues found | Required work · dependencies · next step |
|---|---|---|---|---|---|---|---|---|---|
| X-1 | **New fee types** — delivery coordination, campaign service fee | Missing | — | ✗ | 25 | P1 | S | The registry ([`fees.ts`](../../../STREET-SERVE-APPLICATION-BACKEND/src/modules/payments/fees.ts)) resolves DB-first with code fallback and was built so a new fee is config, not code. Adding to `FEE_TYPES` requires a seed migration | Add types + seed rates. Deps: DAN-8, MB-1. **Next:** one migration covers both |
| X-2 | **New ledger account types** — custodial community funds | Missing | — | ✗ | 30 | P0 | M | See PIF-23. `signedDelta`'s `DEBIT_NORMAL` set must be updated correctly or balances read inverted | Account + entry types + reconciliation. Deps: [ADR-005](ADR-005-custodial-community-funds.md) ✅. **Next:** declare the normal side explicitly per [A-2](ARCHITECTURAL_IMPROVEMENTS.md); shadow-run against `scripts/shadow-run-ledger.ts` before going live |
| X-3 | **New role** — `driver` | Missing | — | ✗ | 20 | P0 | S | `SELF_GRANTABLE_ROLES` is `['seller','vendor','hub']`. A driver must **not** be self-grantable — vetting has to gate it | Add role, exclude from self-grant, gate on verification tier. Deps: ADR-004. **Next:** confirm the memory-noted enum-writer CI gate accepts the new value |
| X-4 | **New realtime channels** — courier position, delivery offers | Missing | ✗ | ✗ | 25 | P1 | M | Seven emit methods exist; none is high-frequency positional. Socket.IO + Redis adapter + test sink are solid foundations | `/delivery` namespace + emitters. Deps: DAN-6. **Next:** budget it — this is the first sustained high-write realtime path on the platform |
| X-5 | **Agreements** — driver terms, contribution terms, PIF terms | Missing | ✗ | ✗ | 30 | P2 | M | [`agreements.registry.ts`](../../../STREET-SERVE-APPLICATION-BACKEND/src/modules/agreements/agreements.registry.ts) is versioned and acceptance-tracked — the right home. Existing entries are still `reviewed: false` pending counsel | Draft + register three agreement types. Deps: ADR-004/005/006. **Next:** they join an existing legal backlog; start early |
| X-6 | **Notification categories** — delivery, generosity, campaign | Missing | ✗ | ✗ | 40 | P2 | S | Preference categories + inbox + push tokens are built and tested | Add categories. Deps: none. **Next:** every new category must be user-mutable |
| X-7 | **CI gates** — reachability, route coverage, enum writers | Needs Fixing | — | ◐ | 50 | P1 | S | Three gates exist with baselines (per Phase 4 architecture). New modules will trip route-coverage and enum-writer baselines | Update baselines as each module lands. Deps: all. **Next:** update per-PR, never in a batch at the end |
